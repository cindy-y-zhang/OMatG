# J: real jointly generated CN-RDF state. Overlay on joint_geometry/configs/mpts52.yaml.
model:
  model:
    init_args:
      encoder:
        init_args:
          geometry_input: true

data:
  shuffle_within_element: false

trainer:
  logger:
    class_path: lightning.pytorch.loggers.CSVLogger
    init_args:
      save_dir: joint_geometry/runs/full/J_seed0
      name: ""
